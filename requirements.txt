# WorthWise — requirements.txt
# Pin versions that must match the training environment exactly.
# xgboost and scikit-learn are pinned to match the pickled artifacts.

streamlit>=1.35.0
xgboost>=2.0.0,<3.3.0
scikit-learn==1.6.1
joblib>=1.3.0
shap>=0.45.0
pandas>=2.0.0
numpy>=1.26.0
google-genai>=1.0.0
python-dotenv>=1.0.0
